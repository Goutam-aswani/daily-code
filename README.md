# Sample Python Script

This small repository contains `sample.py`, a tiny CLI program that prints a greeting.

How to run locally (PowerShell):

```powershell
python .\sample.py Alice
```

Next steps I can take for you:
- Initialize a git repository here and create a local commit (I can do this now).
- Add a remote and push to GitHub (I'll need the remote URL or you can authenticate locally with `gh` or git credentials).
